import 'package:flutter/material.dart';
import 'package:ploop_fe/main.dart';

class WorldPage extends StatelessWidget {
  const WorldPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('World'));
  }
}
